16.22. Fancy Automatic Linking in Contract Entry

  * If we want to get fancy in the Spring -- Is there a way to generate a unique random ID on each dashboard run that links these three together where each record has a Contract Entry User + Contract Entry ID? It has a modify expression to update for the selected user + entry ID


