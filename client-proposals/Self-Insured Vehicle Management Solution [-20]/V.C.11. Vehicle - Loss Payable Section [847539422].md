5.3.11. Vehicle - Loss Payable Section

*Done. 

  


  * Loss Payable section:
    * Payee Name (drop-list of active "Other Organization"-type Contacts; editable for Admins and the Owner's Agent; error on Save if the Vehicle is active and the selected "Payee Name" Contact is not active: "The loss payee '<"Payee Name">' is no longer active.")
    * Address 1 (read-only macro; dynamically displays the Primary "Address" for the selected "Payee Name" Contact)
    * Address 2 (read-only macro; dynamically displays the Primary "Address 2" for the selected "Payee Name" Contact)
    * City (read-only macro; dynamically displays the Primary "City" for the selected "Payee Name" Contact)
    * State (read-only macro; dynamically displays the Primary "State" for the selected "Payee Name" Contact)
    * Zip (read-only macro; dynamically displays the Primary "Zip" for the selected "Payee Name" Contact)
    * Phone (read-only macro; dynamically displays the Primary "Phone" for the selected "Payee Name" Contact)


